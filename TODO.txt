- dialogue lines will be on bottom, covering up the HUD, rather than on top.
  (maybe?)
    - GBAtek notes that the first 8 scanlines are effectively invisible on
      a real GBA, since they are covered by a shadow.
- maybemake substep processing local rather than global, like how it is in the
  code for the original unyuland.
- strange bug i don't know how to replicate, where:
   - inter-entity collision does not work at all anymore
   - pause menu graphics are glitch y
  possible theory is that the oob writes are occuring in IWRAM and messing with
  the physics and printing code... could also be related to the sound system,
  since it only started happening after i finished it.
- need more symbols for the font